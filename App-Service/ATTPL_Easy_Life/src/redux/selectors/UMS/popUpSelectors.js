export const selectPopUpFormDetails = state => state.popUp.popUpFormDetails;

export const selectUpdateStatus = state => state.popUp.updateStatus;

export const selectLoading = state => state.popUp.loading;

export const selectError = state => state.popUp.error;
