export const selectProfileImage = state => state.profileImage.profileImage;

export const selectLoading = state => state.profileImage.loading;

export const selectError = state => state.profileImage.error;
