export const selectImages = state => state.image.images;

export const selectUploading = state => state.image.uploading;

export const selectDeleting = state => state.image.deleting;

export const selectError = state => state.image.error;
